*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# --- Agentic Mobile QA Lab ---
# Test evidence: regenerated by every run, never committed. Keep the directory itself.
/artifacts/*
!/artifacts/.gitkeep

# Maestro's own default output location, if a flow is run outside the wrapper scripts.
/.maestro/

# Per-developer Claude Code settings (permission allowlists etc.).
# .claude/settings.json IS committed on purpose — the hook config is shared.
.claude/settings.local.json
